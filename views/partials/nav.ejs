  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav>
    <a href="/beers">Browse Beers</a>
    <a href="/profiles">View Profiles</a>
    <a href="/profiles">My Profile</a>
    <% if (user) { %>
      <a href="/auth/logout" class="log">Logout</a>
    <% } else { %>
      <a href="/auth/google" class="siwg-link log">
        <button type="submit" class="siwg">
          <img id="siwg-image" alt="Sign in with Google" />
        </button>
      </a>
    <% } %>
  </nav>
